source "https://rubygems.org"

gem "jekyll", "~> 4.3"

# GitHub Pages dependency group
group :jekyll_plugins do
  gem "jekyll-feed", "~> 0.17"
  gem "jekyll-seo-tag", "~> 2.8"
  gem "jekyll-scholar", "~> 6.0" # For publications
  gem "jekyll-remote-theme", "~> 0.4.3" # Required for remote_theme
  # Add other plugins used by al-folio or your customizations
  # gem "jekyll-include-cache"
end

# Specify the theme gem if you install it locally instead of using remote_theme
# gem "al-folio", "~> 0.1.0" # Example version, check for the latest

# Windows does not include zoneinfo files, so bundle the tzinfo-data gem
gem 'tzinfo-data', platforms: [:mingw, :mswin, :x64_mingw, :jruby]
